# Project Notes

This repository documentation was generated from the AWS console and Postman evidence supplied during development.

Before publishing the final repository:
- replace `lambda/lambda_function.py` with the exact deployed code;
- add the final Bedrock model ID;
- confirm the final AWS regions;
- add the final S3 bucket name only if it is safe to expose;
- review IAM policies for least privilege;
- remove any account IDs, secrets, tokens, or sensitive screenshots that should not be public.
